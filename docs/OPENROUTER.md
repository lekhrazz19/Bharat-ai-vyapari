# OpenRouter

Set:

```env
OPENROUTER_API_KEY=
OPENROUTER_DEFAULT_MODEL=google/gemini-2.5-flash
OPENROUTER_FALLBACK_MODEL=google/gemini-2.0-flash-exp:free
```

The service uses OpenAI-compatible `/chat/completions` and `response_format: {"type":"json_object"}` for JSON agents.
